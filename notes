fix total amount.
= fix heatmap - add library to local folder or search for online cdn repositories.
= performance loading all markers, more than 5000 points
= fix pagination
+ add kml format download
+ remove latitude/longitude
+ remove observation_date
+ add link for EOL   = https://eol.org/search_results?utf8=✓&q={}
+ add link for GBIF  = https://www.gbif.org/search?q={}
----
before deploy, download the current version of the portal to an history folder.

actualiser d'abord la table multimedia
actualiser en suite le dictionaire default_imgs = {}

des images:
 = chaque iten de collection a une attribute 'thumbnail_id' correspondent à thumbnail_id dans la table 'multimedia'

Pour ajouter un thumbnail il faut juste:
 -> ensérer un nouvel enregistrement sur la table multimedia:
        (description, type, order, collection_id, provider_id, full_name, short_name)
        ('', 1, NULL, NULL, provider_id, full_name, short_name)]
        ('thumbnail of Instituto de Investigacao Pesqueira', 1, NULL, NULL, 4, 'iip-thumbnail.png', 'thumb_iip')
        où:
        provider_id: se trouve dans la table provider
        full_name: c'est le nom de l'image (sans code_code_) dans le dossier multimedia: par exemple por 1_8_museu-thumbnail.png, serait museu-thumbnail.png
        short_name: c'est un nom court donné: par exemple thumb_museu

        actualisé sur la table collection le valeur thumbnail_id pour le valeur de l'idée créer en pas avant


Pour ajouter un logo il faut juste:
 -> ensérer un nouvel enregistrement sur la table multimedia:
         (description, type, order, collection_id, provider_id, full_name, short_name)
        ('', 4, NULL, NULL, provider_id, full_name, short_name)
        où:
        provider_id: se trouve dans la table provider
        full_name: c'est le nom de l'image (sans code_code_) dans le dossier multimedia: par exemple por 1_8_museu-thumbnail.png, serait museu-thumbnail.png
        short_name: c'est un nom court donné: par exemple thumb_museu


--- to change
 = on the BioNoMo_Service
        -  provider stats
            - group by provider.abbreviation and sum the colletcion count
 = on Harvester
        - test script and make sure each provider with different name gets its own record on the database
 * test the harvester against the production database
        - get production database
        - run harvest against a copy of the production database
  * add a block of code to update the provider multimedia references


  ==
  =
= todo:
 => run the portal without changing any settings
    = and systematically apply the changes on the multimedia for it to change
 => run the harvest again, against the production database and make sure all the multimedia is set right
        = check whether the multimedia for the provider.abbreviation exists on the database
            - if not use the default settings (add a dict on the constantsx)